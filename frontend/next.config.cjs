/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // unfortunately Semantic UI is not ready for strict mode yet, we can re-enable once upgraded to Semantic UI v3
}

module.exports = nextConfig
